import { Routes } from '@angular/router';
import { UploadCityLedgerComponent } from './upload-city-ledger/upload-city-ledger.component';

export const routes: Routes = [
    { path:'upload-c-ledger', component: UploadCityLedgerComponent }
];
